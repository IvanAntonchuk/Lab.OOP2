/**
 * @file testwebutils.h
 * @brief Заголовочний файл для модульного тестування мережевих утиліт.
 */

#ifndef TESTWEBUTILS_H
#define TESTWEBUTILS_H

#include <QObject>
#include <QtTest>

/**
 * @class TestWebUtils
 * @brief Клас модульних тестів для перевірки класу WebUtils.
 *
 * Використовує фреймворк QtTest для перевірки асинхронної логіки отримання
 * заголовків веб-сторінок. Тести перевіряють як успішні сценарії завантаження
 * даних, так і стійкість системи до некоректних URL-адрес або відсутності з'єднання.
 */
class TestWebUtils : public QObject {
    Q_OBJECT

private slots:
    /**
     * @brief Тестує успішне отримання заголовка сторінки.
     * Перевіряє, чи правильно WebUtils ініціює запит, чи випускається сигнал
     * titleReady() і чи відповідає отриманий текст очікуваному заголовку сторінки.
     */
    void testFetchTitleSuccess();

    /**
     * @brief Тестує поведінку при роботі з некоректним URL.
     * Перевіряє, як система реагує на завідомо помилкові адреси, і чи коректно
     * обробляється помилка (наприклад, повернення самого URL замість заголовка),
     * щоб уникнути зависання інтерфейсу.
     */
    void testFetchTitleInvalidUrl();
};

#endif // TESTWEBUTILS_H
